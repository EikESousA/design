import styled from "styled-components";

export const Container = styled.li`
  position: relative;

  padding: var(--spacing-s4) 0 var(--spacing-s8) 0;

  width: 100%;

  display: flex;
  justify-content: flex-start;
  align-items: center;

  background-color: var(--aliases-200);
`;
