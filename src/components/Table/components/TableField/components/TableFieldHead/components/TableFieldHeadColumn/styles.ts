import styled from "styled-components";

export const Container = styled.th`
  padding: 1rem;

  text-align: left;

  font-weight: bold;
  font-size: 0.875rem;

  color: var(--color-gray-900);

  z-index: 1;
`;
