import styled from "styled-components";

export const Container = styled.div`
  position: fixed;
  top: var(--spacing-s4);
  right: var(--spacing-s4);

  padding: 1px;

  z-index: 999;

  overflow: hidden;
`;
