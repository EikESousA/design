import styled from "styled-components";

export const Container = styled.div`
  position: fixed;
`;

export const Empty = styled.div`
  position: fixed;
  top: 0;
  left: 0;

  height: 0;
  width: 0;
`;
